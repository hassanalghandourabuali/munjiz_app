// اختبار بسيط مؤقت — الاختبار الافتراضي الأصلي كان يشير لـ MyApp
// وهو اسم غير موجود بمشروعنا (اسم التطبيق الفعلي مختلف)، فتم استبداله
// باختبار بسيط لا يعتمد على أي شاشة، حتى لا يعطّل عملية البناء.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity check', () {
    expect(1 + 1, 2);
  });
}
